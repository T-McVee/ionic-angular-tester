import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-task-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
